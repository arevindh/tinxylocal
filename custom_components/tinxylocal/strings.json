{
  "config": {
    "step": {
      "user": {
        "title": "Configure Device",
        "data": {
          "host": "[%key:common::config_flow::data::host%]",
          "mqtt_pass": "MQTT Password",
          "device_id": "Device ID",
          "api_key": "[%key:common::config_flow::data::api_key%]"
        }
      },
      "choose_token": {
        "title": "API Token Options",
        "description": "Choose how you would like to proceed with the API token.",
        "data": {
          "token_choice": "Token Option"
        }
      },
      "zeroconf_confirm": {
        "title": "Discovered Tinxy device",
        "description": "A Tinxy device was found at {host} (chip {chip_id}).\n\nEnter your Tinxy API key to match it against your account and add it.",
        "data": {
          "api_key": "[%key:common::config_flow::data::api_key%]"
        }
      }
    },
    "error": {
      "cannot_connect": "[%key:common::config_flow::error::cannot_connect%]",
      "invalid_auth": "[%key:common::config_flow::error::invalid_auth%]",
      "unknown": "[%key:common::config_flow::error::unknown%]",
      "device_not_in_account": "That device is not in the Tinxy account this API key belongs to."
    },
    "abort": {
      "already_configured": "[%key:common::config_flow::abort::already_configured_device%]",
      "api_not_available": "The device answered but its local API is not enabled.",
      "cannot_connect": "[%key:common::config_flow::error::cannot_connect%]"
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Tinxy Local Options",
        "description": "Configure advanced settings for Tinxy Local integration.",
        "data": {
          "host": "Device IP Address",
          "api_key": "API Key",
          "request_timeout": "Request Timeout",
          "polling_interval": "Polling Interval",
          "rate_limit_delay": "Command spacing"
        },
        "data_description": {
          "host": "Local IP address of the Tinxy device",
          "request_timeout": "Timeout in seconds for HTTP requests to Tinxy devices. Default is 3 seconds. Increase if you have slow network connections.",
          "polling_interval": "How often to poll devices for status updates. Default is 6 seconds. Must be greater than or equal to request timeout. Lower values provide faster updates but increase network traffic.",
          "rate_limit_delay": "Minimum gap between commands to one device. Default is 1 second, which is the fastest the device's authentication allows: its token encrypts a whole-second timestamp, so two commands in the same second cannot both be accepted. This applies per device, not per switch. Raise it if a device stops responding."
        }
      }
    },
    "error": {
      "invalid_auth": "[%key:common::config_flow::error::invalid_auth%]",
      "cannot_connect": "[%key:common::config_flow::error::cannot_connect%]",
      "unknown": "[%key:common::config_flow::error::unknown%]",
      "polling_less_than_timeout": "Polling interval must be greater than or equal to request timeout"
    }
  }
}
